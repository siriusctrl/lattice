{
  "name": "@lattice/acp-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "lattice-acp-sidecar": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "probe": "npm run build && node scripts/probe.mjs"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
